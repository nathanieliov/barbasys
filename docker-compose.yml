version: '3.8'

services:
  barbasys:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/backend
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
